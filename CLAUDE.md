# Package Manager
Use bun for this project. Global bun preference applies.
`mcp-oauth-provider` is Bun-only (uses `Bun.serve()` and `Bun.file()`) — Node.js is not viable.

# References
Before working with Notion MCP tools, read REFERENCES.md for verified tool schemas, parameter quirks, and the enhanced markdown spec access method.
